import { Meta } from '@storybook/addon-docs/blocks'

import { CodeTabs, DocsHero, SpecTable } from '../_kit'

<Meta title="Phenix UI/About/Install" />

<DocsHero
  colorPalette="brand"
  eyebrow="About"
  title="Install"
  lead="Phenix UI is fictional, but the installation model mirrors a Chakra UI v3 package."
/>

## Package Manager

<CodeTabs
  tabs={[
    { value: 'npm', label: 'npm', code: 'npm install phenix-ui @chakra-ui/react @emotion/react next-themes' },
    { value: 'yarn', label: 'yarn', code: 'yarn add phenix-ui @chakra-ui/react @emotion/react next-themes' },
    { value: 'pnpm', label: 'pnpm', code: 'pnpm add phenix-ui @chakra-ui/react @emotion/react next-themes' },
  ]}
/>

## Provider

```tsx
'use client'

import { PhenixProvider } from 'phenix-ui'

export function Providers({ children }: { children: React.ReactNode }) {
  return <PhenixProvider>{children}</PhenixProvider>
}
```

## Import Components

```tsx
import { Button, Field, Input } from 'phenix-ui'

export function ExampleForm() {
  return (
    <Field.Root required>
      <Field.Label>
        Email <Field.RequiredIndicator />
      </Field.Label>
      <Input type="email" />
      <Button colorPalette="ember">Save</Button>
    </Field.Root>
  )
}
```

## Peer Dependency Notes

<SpecTable
  columns={[
    { key: 'dependency', header: 'Dependency', width: '28%', isCode: true },
    { key: 'why', header: 'Why it is needed' },
  ]}
  rows={[
    { dependency: '@chakra-ui/react', why: 'Provides the component runtime and style system.' },
    { dependency: '@emotion/react', why: 'Required by Chakra UI v3.' },
    { dependency: 'next-themes', why: 'Powers color mode in Next.js applications.' },
  ]}
/>
