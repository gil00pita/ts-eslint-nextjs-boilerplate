import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="Phenix UI/About/Getting Started" />

# Getting Started

Start with the smallest useful slice: install the package, wrap the app with the provider, then replace one repeated interface pattern with Phenix UI components.

## Recommended First Steps

1. Install `phenix-ui` and its required peer dependencies.
2. Add `PhenixProvider` at the app root.
3. Import global fonts if your app does not already provide them.
4. Replace one shared pattern, such as a page header, alert, empty state, or form section.
5. Check keyboard navigation, focus states, and color contrast before rolling out more components.

## First Component

```tsx
import { Button, PageHeader, StatusBadge } from 'phenix-ui'

export function AccountPageHeader() {
  return (
    <PageHeader.Root>
      <PageHeader.Content>
        <PageHeader.Eyebrow>Account</PageHeader.Eyebrow>
        <PageHeader.Title>Renewal review</PageHeader.Title>
        <PageHeader.Description>
          Check outstanding actions before confirming the account renewal.
        </PageHeader.Description>
      </PageHeader.Content>
      <PageHeader.Actions>
        <StatusBadge colorPalette="moss">Ready</StatusBadge>
        <Button colorPalette="ember">Continue</Button>
      </PageHeader.Actions>
    </PageHeader.Root>
  )
}
```

## Adoption Checklist

<table>
  <thead>
    <tr>
      <th>Step</th>
      <th>Done when</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Inventory</td>
      <td>You know which local components overlap with Phenix UI.</td>
    </tr>
    <tr>
      <td>Token mapping</td>
      <td>Existing colors, spacing, and typography have token equivalents.</td>
    </tr>
    <tr>
      <td>First route</td>
      <td>One production route uses the provider and selected components.</td>
    </tr>
    <tr>
      <td>QA</td>
      <td>Designers, engineers, and accessibility reviewers have checked the route.</td>
    </tr>
  </tbody>
</table>
