import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="Phenix UI/Guidelines/Patterns" />

# Patterns

Patterns describe reusable product flow shapes. Use them when a component alone is not enough to define the experience.

## Core Patterns

<table>
  <thead>
    <tr>
      <th>Pattern</th>
      <th>Use when</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Empty state</td>
      <td>A user has no data, no results, or no permission to view content.</td>
    </tr>
    <tr>
      <td>Error recovery</td>
      <td>A task failed and the user needs a next step.</td>
    </tr>
    <tr>
      <td>Progressive disclosure</td>
      <td>Details are useful but not always needed.</td>
    </tr>
    <tr>
      <td>Review and confirm</td>
      <td>An action has meaningful consequences.</td>
    </tr>
    <tr>
      <td>Filtering and search</td>
      <td>Users need to narrow a large data set.</td>
    </tr>
    <tr>
      <td>Bulk action</td>
      <td>Users need to apply one action to multiple selected items.</td>
    </tr>
  </tbody>
</table>

## Pattern Anatomy

Each pattern should document:

- When to use it.
- When to avoid it.
- Required components.
- Required content.
- Keyboard and focus behavior.
- Empty, loading, success, and error states.

## Product Fit

Use the pattern as a starting point. Adjust density, language, and confirmation steps based on user risk, task frequency, and the cost of making a mistake.
