import { Meta } from '@storybook/addon-docs/blocks'

import { Callout, DocsHero, SpecTable } from '../_kit'

<Meta title="Phenix UI/Guidelines/Patterns" />

<DocsHero
  colorPalette="orange"
  eyebrow="Guidelines"
  title="Patterns"
  lead="Patterns describe reusable product flow shapes. Use them when a component alone is not enough to define the experience."
/>

## Core Patterns

<SpecTable
  columns={[
    { key: 'pattern', header: 'Pattern', width: '26%' },
    { key: 'use', header: 'Use when' },
  ]}
  rows={[
    { pattern: 'Empty state', use: 'A user has no data, no results, or no permission to view content.' },
    { pattern: 'Error recovery', use: 'A task failed and the user needs a next step.' },
    { pattern: 'Progressive disclosure', use: 'Details are useful but not always needed.' },
    { pattern: 'Review and confirm', use: 'An action has meaningful consequences.' },
    { pattern: 'Filtering and search', use: 'Users need to narrow a large data set.' },
    { pattern: 'Bulk action', use: 'Users need to apply one action to multiple selected items.' },
  ]}
/>

<Callout title="Pattern Anatomy" tone="tip">
Each pattern should document:

- When to use it.
- When to avoid it.
- Required components.
- Required content.
- Keyboard and focus behavior.
- Empty, loading, success, and error states.
</Callout>

## Product Fit

Use the pattern as a starting point. Adjust density, language, and confirmation steps based on user risk, task frequency, and the cost of making a mistake.
