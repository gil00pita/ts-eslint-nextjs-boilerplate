import { Meta } from '@storybook/addon-docs/blocks'

import { Callout, DocsHero, SpecTable } from '../_kit'

<Meta title="Phenix UI/Brand Styles/Available Components" />

<DocsHero
  colorPalette="teal"
  eyebrow="Brand Styles"
  title="Available Components"
  lead="The component catalogue is grouped by product job rather than implementation detail."
/>

## Inventory

<SpecTable
  columns={[
    { key: 'group', header: 'Group', width: '18%' },
    { key: 'components', header: 'Components', isCode: true },
    { key: 'status', header: 'Status', width: '16%', badgeTone: (value) => (value === 'Stable' ? 'success' : 'warning') },
  ]}
  rows={[
    { group: 'Actions', components: 'Button, IconButton, ActionBar, SplitButton', status: 'Stable' },
    { group: 'Forms', components: 'Field, Input, Textarea, Select, Checkbox, Radio, Switch', status: 'Stable' },
    { group: 'Feedback', components: 'Alert, Toast, EmptyState, Progress, Skeleton', status: 'Stable' },
    { group: 'Navigation', components: 'Breadcrumb, Tabs, SideNav, Pagination', status: 'Beta' },
    { group: 'Data display', components: 'Table, DataCard, Stat, Timeline, StatusBadge', status: 'Beta' },
    { group: 'Overlays', components: 'Dialog, Drawer, Popover, Tooltip, Menu', status: 'Stable' },
    { group: 'Layout', components: 'Page, PageHeader, Stack, Grid, Container', status: 'Stable' },
  ]}
/>

## Maturity Levels

<SpecTable
  columns={[
    {
      key: 'status',
      header: 'Status',
      width: '20%',
      badgeTone: (value) =>
        value === 'Stable' ? 'success' : value === 'Beta' ? 'info' : value === 'Deprecated' ? 'danger' : 'neutral',
    },
    { key: 'meaning', header: 'Meaning' },
  ]}
  rows={[
    { status: 'Draft', meaning: 'API and design are still changing.' },
    { status: 'Beta', meaning: 'Ready for product pilots, but feedback may change details.' },
    { status: 'Stable', meaning: 'Safe for broad adoption with documented migration support.' },
    { status: 'Deprecated', meaning: 'Supported only while teams migrate to a replacement.' },
  ]}
/>

<Callout title="Component Acceptance Criteria" tone="tip">
- Uses Phenix semantic tokens and recipes.
- Supports keyboard interaction and visible focus states.
- Includes empty, loading, disabled, invalid, and overflow states where relevant.
- Has documentation for when to use it and when to avoid it.
</Callout>
