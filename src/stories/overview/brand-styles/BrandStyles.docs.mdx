import { Meta } from '@storybook/addon-docs/blocks'
import { Box, Grid, Text } from '@chakra-ui/react'

import { DocsHero, SpecTable } from '@/components/_storybook'

<Meta title="Phenix UI/Brand Styles/Overview" />

<DocsHero
  colorPalette="teal"
  eyebrow="Brand Styles"
  title="Brand Styles"
  lead="A practical visual system for interface-heavy products. The identity is warm, crisp, and durable: enough character to be recognizable, but restrained enough for long work sessions."
/>

## Foundations

<SpecTable
  columns={[
    { key: 'foundation', header: 'Foundation', width: '26%' },
    { key: 'purpose', header: 'Purpose' },
  ]}
  rows={[
    {
      foundation: 'Available Components',
      purpose: 'Shows the current component inventory and maturity level.',
    },
    {
      foundation: 'Color',
      purpose: 'Defines palettes, semantic roles, and contrast expectations.',
    },
    { foundation: 'Icons', purpose: 'Explains icon sizing, stroke weight, and accessibility use.' },
    { foundation: 'Illustrations', purpose: 'Sets the tone for product and empty-state artwork.' },
    { foundation: 'Motion', purpose: 'Keeps state changes understandable and respectful.' },
    { foundation: 'Grid', purpose: 'Provides layout structure across breakpoints.' },
    { foundation: 'Spacing', purpose: 'Creates rhythm using predictable token steps.' },
    { foundation: 'Radius', purpose: 'Sets shape language for controls, panels, and surfaces.' },
    { foundation: 'Themes', purpose: 'Documents light, dark, and high-contrast behavior.' },
    { foundation: 'Typography', purpose: 'Defines type scale, hierarchy, and reading patterns.' },
  ]}
/>

## Visual Personality

Phenix UI should feel:

<Grid gap={3} my={6} templateColumns={{ base: '1fr', sm: 'repeat(2, 1fr)' }}>
  {[
    { yes: 'Focused', no: 'not severe' },
    { yes: 'Warm', no: 'not playful' },
    { yes: 'Modern', no: 'not trend-led' },
    { yes: 'Efficient', no: 'not cramped' },
  ].map((pair) => (
    <Box
      key={pair.yes}
      bg="colorPalette.subtle"
      borderRadius="lg"
      colorPalette="teal"
      px={4}
      py={3}
    >
      <Text color="colorPalette.fg" fontSize="lg" fontWeight="semibold">
        {pair.yes}
      </Text>
      <Text color="fg.muted" fontSize="sm">
        {pair.no}
      </Text>
    </Box>
  ))}
</Grid>
