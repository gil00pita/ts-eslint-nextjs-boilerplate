import { Meta, Canvas, Controls, Unstyled } from '@storybook/addon-docs/blocks'
import * as DesignSystemStories from './DesignSystem.stories'
import { CodeBlock, Table } from '@chakra-ui/react'

<Meta title="Brand X/Styles/Design System" of={DesignSystemStories} />

# Design System

The Brand X Design System is a set of design patterns and components that are used to create consistent and accessible user interfaces for government services.

<Canvas of={DesignSystemStories.ColorPalette} />
